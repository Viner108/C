#include <iostream>
#include <array>
#include <vector>

template<typename T, size_t N>
struct array
{
    
};
/*
template<size_t M, size_t N, typename Field = Rational>
struct Matrix
{
    
};

template<size_t M, size_t N,size_t K, typename Field = Rational>
Matrix<M,K,Field> operator*(const Matrix<M,N,Field>&, const Matrix<N,K,Field>&);


template<typename T,template <typename> Container = std::vector<T>>
class Stack{
    Container<T> c;
    public:

};

*/
int main(){

    //Stack<int> s;

    //const int x = 5;
    //std::cin >> x;

    //const int y = x;

    //std::array<int, x> a;

    //std::array<int, 5> a;
    //std::array<int, 1000> aa;
    //std::cout << sizeof(aa);


}